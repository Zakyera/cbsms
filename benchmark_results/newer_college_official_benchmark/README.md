# Official Newer College CBS Benchmark Results

Each `sequences/<sequence>/<mode>` directory is one official result slot. Only entries with `status: COMPLETE` are admitted to the paper table. Pilot and partial runs belong under `pilots/`.

Run `src/cbsms/tools/newer_college_results.py collect --root <this-dir>` after finalizing any result.
